# 防災アプリUI移植用プロンプト

以下の指示を、移植先システムのAIに1回だけ入力してください。

```text
あなたは既存システムのコードを調査して実装できるシニアエンジニアです。
この指示の対象は、既存システム全体ではなく、次の3つのUI機能だけです。

1. 避難所検索結果表示画面
2. ホーム画面の避難所マップと気象警報・注意報表示
3. ファビコン

既存の認証、管理者メニュー、指示ボード、避難所登録、データ保存、その他の画面や機能は変更しないでください。移植先のフレームワーク、ルーティング方式、テンプレート方式、CSS/JavaScript構成に合わせて実装し、既存の命名規則とデザインをできるだけ尊重してください。

## 目標とする見た目

防災情報を確認しやすい、淡いグレーを基調にした日本語UIにしてください。

- ページ背景は淡いグレー（目安: #edf0f3）。
- ヘッダーは淡いグレーで、黒い下線を付ける。
- ナビゲーションには「ホーム」「避難所検索」を表示する。
- ボタンは水色系の背景、黒い枠線、角丸をほぼ使わない四角い外観にする。
- 検索結果は中央寄せの横幅約980px以内に配置する。
- 表は黒に近い細い罫線で区切り、列は「避難所名」「開設状況」「混雑状況」「詳細」とする。
- 画面幅が狭い場合も横方向にはみ出しにくく、表やフォームを使いやすくする。
- モーダルを開いても、背後のページと内容が不自然に重ならないようにする。

## 1. 避難所検索結果表示画面

検索結果ページを追加または既存ページを置き換えて、次の仕様を実装してください。

### ルートと検索

- GETの検索結果ページを用意する。パスは移植先の既存ルーティング規則に合わせる。
- クエリパラメータ `keyword` を受け取る。既存システムが `address` や `name` を使っている場合は、それらも互換的に受け付けてよい。
- 入力欄のプレースホルダーは「住所または避難所名」、検索ボタンの表示は「検索結果表示」とする。
- キーワードが空の場合は、一覧表示できる全候補を表示する。
- 検索対象は少なくとも避難所名、住所、開設状況とする。
- 大文字小文字と空白の違いを吸収して部分一致検索する。
- 該当なしの場合は「該当する避難所が見つかりませんでした」と表示する。
- 既存の本番データ取得処理がある場合はそれを利用し、デモデータを勝手に本番データへ置き換えない。

### 一覧表

各結果を次の4列で表示してください。

- 避難所名
- 開設状況
- 混雑状況
- 詳細

各行には避難所の一意なIDを保持し、「詳細な情報を見る」ボタンを置いてください。混雑状況が空の場合は「不明」と表示します。テンプレートを使う場合は、表示値をHTMLとして直接解釈せず、必ずエスケープしてください。

### 詳細モーダル

「詳細な情報を見る」を押すと、同じページ上に詳細モーダルを表示してください。ページ遷移や別画面への移動は不要です。

モーダルには次の項目を表示してください。存在しない値は「なし」と表示します。

- 避難所名
- 住所
- 開設状況
- 混雑状況
- 連絡先
- 障がいをお持ちの方の対応
- 外国語（英語）の対応
- ペットの同伴
- 備考

さらに、モーダル上部に次の領域を用意してください。

- マップ表示領域（実際の地図機能が既存システムにある場合はそれを利用し、ない場合は「マップ」と表示するプレースホルダーでよい）
- 写真表示領域（写真がない場合は「写真なし」）

モーダルには「閉じる」ボタンを置き、次の操作で閉じられるようにしてください。

- 閉じるボタンを押す
- モーダル外側の暗い背景を押す
- 可能ならEscapeキーを押す

モーダルの開閉状態は、スクリーンリーダーでも分かるように `aria-hidden`、`role="dialog"`、`aria-modal="true"` などを適切に設定してください。ボタンと入力欄はキーボードで操作できるようにしてください。

## 2. ホーム画面

既存のホーム画面に、次の2つのセクションを追加または整備してください。既存のホーム画面にある無関係な機能は壊さないでください。

### 避難所マップ

- 見出しは「避難所マップ」とする。
- 地図領域をカード状のセクション内に配置する。
- 既存システムでLeafletが利用可能、または外部CDNを利用できる場合はLeaflet 1.9.4を使う。
- Leafletを使う場合は、CSSとJavaScriptを読み込み、地図タイルにOpenStreetMapを使い、帰属表示を付ける。
- 地図領域には安定した高さを設定する。目安はデスクトップ410px、モバイル310px。
- 地図の初期表示位置は、既存システムの対象地域に合わせる。対象地域が青森市の場合は緯度35.3390、経度139.4903、ズーム12を初期値とする。
- 対象地域や避難所座標が移植先で異なる場合は、移植先のデータを優先し、座標をハードコードしたままにしない。
- 地図ライブラリが使えない環境では、画面全体を壊さず、地図領域をプレースホルダーとして表示する。

### 気象警報・注意報

- 見出しは「青森市 気象警報・注意報」とする。ただし移植先の対象地域が異なる場合は、その地域名に置き換える。
- 「10分おき自動更新」という表示を置く。
- 対象地域名を表示する。
- 警報・注意報がない場合は「現在、警報・注意報は発表されていません」と表示する。
- 警報・注意報がある場合は「警報・注意報が発表されています」と表示し、警報名と状態（発表・継続など）を一覧表示する。
- データ取得時刻と気象庁発表時刻を表示する。
- 既存システムに気象情報APIがある場合はそれを利用する。APIがない場合は、ホーム画面が壊れないように固定の空状態または取得失敗状態を表示する。
- API通信失敗時もJavaScriptエラーだけで画面全体を壊さず、取得失敗を分かる形で表示する。
- 初回表示時に取得し、その後600000ミリ秒（10分）ごとに更新する。
- APIから受け取った値を画面へ挿入する際は、HTMLとして解釈させる必要がない値には `textContent` などを使い、XSSが起きないようにする。

## 3. ファビコン

- 移植先の静的ファイル配置に合わせてファビコンを追加する。
- 現在の完成形では `static/favicon.ico` に256x256のICO画像を配置している。
- 全ページ共通のHTMLの `<head>` に、次の意味のリンクを追加する。

  `<link rel="icon" href="静的ファイルへのURL/favicon.ico" type="image/x-icon">`

- 移植先のヘルパーやアセットパイプラインがある場合は、それを使って正しいURLを生成する。
- ファビコンが見つからない場合に404になるパスを残さない。

## 実装上の注意

- まず既存コードを調査して、同じ役割のルート、テンプレート、静的ファイル、データ構造を特定する。
- 変更対象は検索結果画面、ホーム画面、共通ヘッダーのファビコン設定、およびそれらを動かすために不可欠な最小限のバックエンド処理に限定する。
- 既存のAPI、データベース、認証、他画面のレイアウトを不要に変更しない。
- 既存の避難所データ項目名を優先する。対応する項目がない場合だけ、以下の意味にマッピングする。
  - `id`: 一意なID
  - `name`: 避難所名
  - `address`: 住所
  - `status`: 開設状況
  - `crowding`: 混雑状況
  - `phone`: 連絡先
  - `support`: 障がいをお持ちの方への対応
  - `pet`: ペットの同伴
  - `note`: 備考
  - `photo`: 写真または写真URL
- 既存のフィールド名が異なる場合は、バックエンドで変換するか、テンプレート側で明確な対応表を作り、詳細モーダルが空欄だらけにならないようにする。
- 外部CDNを使う場合は、既存プロジェクトの方針に従い、利用できない場合の表示崩れを抑える。
- 新しい依存関係を追加する場合は、既存の依存関係と競合しないことを確認し、必要なら依存関係ファイルも更新する。

## 完了条件

実装後、次の確認を自動または手動で行ってください。

1. ホーム画面が表示でき、地図領域と気象警報・注意報セクションが表示される。
2. 気象情報APIの成功、警報あり、空データ、通信失敗の各状態で画面が崩れない。
3. 検索結果ページがキーワードなしで表示できる。
4. 避難所名、住所、開設状況による部分一致検索が動く。
5. 該当なしのメッセージが表示される。
6. 各行の「詳細な情報を見る」から正しい避難所の詳細モーダルが開く。
7. モーダルをボタン、背景クリック、可能ならEscapeキーで閉じられる。
8. スマートフォン幅でもヘッダー、検索フォーム、表、モーダルの文字が重ならない。
9. 全ページでファビコンが読み込まれ、ファビコンの404が発生しない。
10. 既存の対象外画面と既存テストが壊れていない。

最後に、変更したファイル、実装した機能、実行した検証コマンドと結果だけを簡潔に報告してください。要求を満たすために必要なコード変更は、確認を求めずこの指示内で完了させてください。
```

## 移植時に置き換える値

移植先の対象地域や既存データに応じて、プロンプト内の次の値だけは置き換えてください。

- `青森市`: 移植先の対象地域名
- `35.3390, 139.4903`: 移植先の地図初期座標
- 気象情報APIのURLやレスポンス形式
- 避難所データのフィールド名
