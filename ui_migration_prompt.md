# 防災アプリ移植用プロンプト

以下のコードブロック内を、移植先システムのAIに1回だけ入力してください。

```text
あなたは既存システムを調査し、同等の機能を実装できるシニアエンジニアです。
この指示を最初に1回だけ読み、実装・検証・必要な修正まで完了してください。途中で確認を求めず、移植先の既存コードを優先して判断してください。

# 目的

現在の完成形と同等の、日本語の防災情報アプリを移植してください。移植先に同じ役割の機能がある場合は再利用し、無関係な機能・データ・認証・画面を壊さないでください。移植先のフレームワーク、ルーティング、テンプレート、データベース、静的ファイル構成に合わせて実装してください。

# 共通UI

- ページ背景は淡いグレー、コンテンツは白、文字と罫線は黒に近い色にする。
- 共通ヘッダーに「青森市防災システム」を表示する。移植先の地域が異なる場合は地域名を置き換える。
- 共通ナビゲーションに「ホーム」「避難所検索」「文字の大きさの変更」「管理者向けメニュー」を置く。
- 文字サイズは小・標準・大を選べ、可能なら選択状態を保存する。
- ボタンは淡い水色背景、黒い枠線、角丸5px程度を基本とする。
- 画面幅が狭い場合は1列レイアウトに切り替え、文字やボタンを重ねない。
- 全ページのheadに静的ファイルのファビコンを設定する。例: `static/favicon.ico`。
- 表示値はHTMLとして直接解釈せず、テンプレートのエスケープやtextContentを使ってXSSを防ぐ。
- 適切なlabel、aria属性、focus状態を設定し、キーボードで操作できるようにする。

# データ

既存データの項目名を優先し、次の意味に対応づけてください。

- 避難所: `id`, `name`, `status`, `address`, `capacity`, `occupants`, `consideration_count`, `english_support`, `phone`, `email`, `pet`, `supplies`, `latitude`, `longitude`, `photo`, `created_at`, `updated_at`
- 発信情報: `id`, `target`, `message_type`, `content`, `region`, `latitude`, `longitude`, `audience`, `shelter`, `status`, `created_at`, `updated_at`
- 避難所ステータスは「開設」「一時的に閉鎖」「閉鎖」。
- 人数は0以上の整数、現在の入所者は収容人数以下にする。
- 保存先は移植先の既存DBまたはデータ保存方式を使う。

# ホーム画面

避難所マップと防災情報を表示してください。

## 避難所マップ

- 「避難所マップ」と表示する。
- 利用できればLeaflet 1.9.4とOpenStreetMapを使い、帰属表示を付ける。利用できない場合は地図プレースホルダーを表示する。
- 地図は安定した高さにする。目安はデスクトップ590px以内、モバイル430px程度。
- 避難所の緯度経度をマーカー表示し、開設・一時的に閉鎖・閉鎖を色分けする。
- 被害情報に緯度経度があれば別色のマーカーを表示する。
- マーカーのポップアップに避難所名、ステータス、住所、収容人数、現在の入所者、ペット、写真を表示する。

## 防災情報

- 「警報情報（10分ごとに自動更新）」「避難情報」「被害情報」を表示する。
- 避難情報・被害情報は、発信日時などの補足情報を小さく、内容を大きく太字で表示する。項目間は区切り線で分ける。
- 被害情報は発信日時、地域、被害内容を表示する。
- 避難情報は発信日時、対象、避難先、避難指示内容を表示する。
- 情報が多い場合は欄内を縦スクロールできるようにする。
- 情報がない場合は「指示はありません」「被害情報はありません」と表示する。

## 気象警報・注意報

- 対象地域は移植先の地域を使う。初期値は青森市。
- 既存の気象情報APIを使い、初回表示後600000ミリ秒（10分）ごとに再取得する。
- 「最終更新: 年月日 時刻」を表示する。
- 警報ごとに「発表日時」と警報名を、被害情報と同じ通知形式で表示する。
- APIレスポンスに過去の発表が含まれる場合は、最新の発表日時の状態だけを採用する。
- 「発表」「継続」だけを表示し、「発表警報・注意報はなし」など解除状態は表示しない。
- 警報解除時は次回取得で自動削除し、レベル変更時は古いレベルを残さず最新レベルへ置き換える。
- API通信失敗時は「情報取得失敗」を表示し、画面全体を壊さない。

# 避難所検索・検索結果

- 避難所名選択、住所検索、現在地取得、地図を表示する。
- 「避難所が分からない」を選ぶと住所入力・地図選択で検索できる。
- 「検索結果表示」ボタンで避難所名・住所・開設状況を部分一致検索する。
- 空検索では全候補を表示し、該当なしの場合は「該当する避難所が見つかりませんでした」と表示する。
- 検索画面の地図は高さ320px程度にする。
- 結果表の列は「避難所名」「開設状況」「混雑状況」「距離（距離検索時のみ）」「詳細」とする。
- 混雑状況は「現在の入所者 / 収容人数」、値がなければ「不明」とする。
- 各行に「詳細な情報を見る」ボタンを置く。
- 詳細ボタンで同じページ内のモーダルを開き、避難所名、住所、開設状況、混雑状況、連絡先、障がいをお持ちの方への対応、外国語（英語）の対応、ペットの同伴、備考を表示する。空値は「なし」とする。
- モーダル上部に所在地マップと写真領域を置き、写真がなければ「写真なし」と表示する。
- 閉じるボタン、背景クリック、可能ならEscapeキーで閉じる。
- `role="dialog"`, `aria-modal="true"`, `aria-hidden` を適切に設定する。
- 管理者ログイン中は各詳細情報の横に「編集」ボタンを表示し、未ログインでは表示しない。
- 検索画面から検索結果へ遷移してもログイン状態を判定し、管理者の編集ボタンを常に動作させる。

# 管理者ログイン

- 管理者ログイン画面を用意する。
- 未ログインで管理者専用画面へアクセスした場合はログインへリダイレクトする。
- ログイン成功後は元の画面へ戻せるようにする。
- 管理者メニューはログイン中なら「ログアウト」、未ログインなら「ログイン」を表示する。
- 認証情報は移植先の安全な方式を使い、サンプルの固定パスワードを本番用に残さない。

# 避難所登録・更新

- 管理者だけがアクセスできる登録画面を用意する。
- 避難所名、ステータス、住所、収容人数、現在の入所者、要配慮者人数、英語対応、電話、メール、ペット、物資状況、緯度、経度を入力できる。
- 地図クリックで座標を設定し、利用できれば住所からジオコーディングする。
- 新規登録、既存データ更新、避難所削除を実装する。
- 更新時は既存写真の削除と新しい写真への差し替えを可能にする。
- 登録・更新後は成功メッセージを表示する。
- 下部に「一覧へ戻る」と「ホームに戻る」を置く。

## 写真

- `multipart/form-data` で写真を受け付け、上限は5MB程度にする。
- JPEG、PNG、GIF、WebPを許可する。
- MIMEタイプや拡張子だけで拒否しない。JPEGの `.jpg`, `.jpeg`, `.jpe`, `.jfif`、大文字拡張子、`image/jpg`, `image/pjpeg`、MIME空欄などの表記ゆれを受け付ける。
- 実ファイルのシグネチャまたは画像ライブラリで実形式を判定する。
- 保存時は検出した形式の拡張子を使い、ファイル名は安全なランダム名に変更する。
- 内容が画像でない偽装ファイルは拒否する。ブラウザ側とサーバー側の両方で検証する。

# 指示・発信ボード

- 管理者専用画面として避難所マップ、入力・発信、発信内容を表示する。
- 被害内容フォームは住所、地図上の位置、被害内容を入力できる。
- 避難指示フォームは対象、避難指示内容、避難先を入力できる。
- 発信内容を「被害内容」と「避難指示内容」に分ける。
- 本文は大きめの太字、日時・対象・地域・避難先は小さめの通常文字にする。
- 両一覧は独立した縦スクロール領域にする。
- 各項目は日時などの補足情報、本文、区切り線の形式で表示する。
- 各項目に確認付き削除ボタンを置く。
- 下部に「ホームに戻る」を置く。

# 共通ホームボタン

避難所検索、検索結果、避難所登録、指示ボードに同じボタンを置く。

- 文言は「ホームに戻る」
- ホーム画面へのリンク
- 淡い水色背景（目安 `#bdeafa`）
- 黒い2px枠線、角丸5px程度
- 幅150px、高さ46px程度、文字は中央揃えで太字、下線なし
- hover/focus時は少し濃い水色
- モバイルでも画面内に収める

# 実装と検証

1. 既存コードからルート、テンプレート、コンポーネント、データ構造、認証、API、静的ファイルを特定する。
2. 既存実装を拡張し、移植先の命名規則とデザインを尊重する。
3. 上記の全機能を実装する。地域、座標、避難所データ、気象APIは移植先の値を優先する。
4. 外部APIが使えなくても空状態または取得失敗状態で画面を表示する。
5. 実装後、必要なら修正して次を確認する。

# 完了条件

- ホーム、検索、検索結果、ログイン、登録、指示ボードが表示できる。
- 気象APIの成功、警報あり、空データ、通信失敗で画面が崩れない。
- 警報が10分ごとに更新され、解除時に消え、レベル変更時に最新状態へ置き換わる。
- 検索、該当なし、詳細モーダル、モーダルの各閉じ方が動作する。
- 管理者ログイン後、検索結果の編集ボタンが常に表示・動作する。
- 避難所の登録、更新、削除、写真添付、写真削除が動作する。
- 正常なJPEGの表記ゆれを受け付け、偽装ファイルを拒否する。
- 指示ボードの発信、表示、削除が動作する。
- 4画面のホームボタンのデザインが統一されている。
- スマートフォン幅でも文字、表、地図、モーダル、ボタンが重ならない。
- 全ページでファビコンが読み込まれ、既存テストと対象外画面が壊れていない。

最後に、変更したファイル、実装した機能、実行した検証コマンドと結果を簡潔に報告してください。
```
